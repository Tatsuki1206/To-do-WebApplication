# Claude へのルール

## 🚫 禁止操作
- ファイル・ディレクトリの削除は**絶対に行わない**
  - 禁止コマンド: `rm`, `rmdir`, `unlink`, `shutil.rmtree`, `fs.unlink`, `os.remove` など
- ユーザーの確認なしに既存ファイルを上書きしない

## ✅ 変更が必要な場合のルール
- 削除が必要と判断したときは、コマンドを実行せず理由とともにユーザーに報告する
- 既存ファイルを編集する前に、変更差分を提示して承認を得る

## 📁 触ってはいけないファイル・ディレクトリ
- `.env` 系ファイル（`.env`, `.env.local` など）
- `node_modules/`
- `dist/`, `build/`